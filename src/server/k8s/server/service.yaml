apiVersion: v1
kind: Service
metadata:
  name: mallard-server
  namespace: mallard
spec:
  selector:
    app: mallard-server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: mqtt-ws
      port: 8083
      targetPort: 8083
  type: ClusterIP
