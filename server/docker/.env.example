# Copy this file to .env and fill in all required values.
# Never commit .env to version control.

# ── InfluxDB ──────────────────────────────────────────────────────────────────
INFLUX_ADMIN_USER=admin
INFLUX_ADMIN_PASSWORD=change-me-strong-password
INFLUX_TOKEN=change-me-influx-api-token
INFLUX_ORG=mallard
INFLUX_BUCKET=metrics

# ── Mallard server ────────────────────────────────────────────────────────────
# Comma-separated API keys — sent as X-API-Key header by the extension
API_KEYS=change-me-api-key-1,change-me-api-key-2
LOG_LEVEL=INFO
RATE_LIMIT=60/minute

# ── Grafana ───────────────────────────────────────────────────────────────────
GF_SECURITY_ADMIN_PASSWORD=change-me-grafana-password

# ── Caddy TLS ─────────────────────────────────────────────────────────────────
# Leave SERVER_DOMAIN=localhost for a self-signed dev cert.
# Set to your real hostname + ACME_EMAIL for automatic Let's Encrypt.
SERVER_DOMAIN=localhost
ACME_EMAIL=

# ── MQTT (optional) ───────────────────────────────────────────────────────────
# Only needed if using MQTT export from the Mallard extension.
# Create the password file first: docker compose run --rm mosquitto mosquitto_passwd \
#   -c /mosquitto/config/passwd mallard
MQTT_USERNAME=mallard
MQTT_PASSWORD=change-me-mqtt-password
# To disable MQTT ingest entirely, set MQTT_BROKER_URL to empty:
# MQTT_BROKER_URL=
