# Copy this file to .env and fill in all required values.
# Never commit .env to version control.

# InfluxDB
INFLUX_ADMIN_USER=admin
INFLUX_ADMIN_PASSWORD=change-me-strong-password
INFLUX_TOKEN=change-me-influx-api-token
INFLUX_ORG=mallard
INFLUX_BUCKET=metrics

# Mallard server — comma-separated keys sent in X-API-Key header
API_KEYS=change-me-api-key-1,change-me-api-key-2
LOG_LEVEL=INFO
RATE_LIMIT=60/minute

# Grafana
GRAFANA_PASSWORD=change-me-grafana-password
